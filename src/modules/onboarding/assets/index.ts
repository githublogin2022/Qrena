import { Image } from 'react-native';

export const images = {
  illustration1: Image.resolveAssetSource(require('./png/illustration-1.png')),
  illustration2: Image.resolveAssetSource(require('./png/illustration-2.png')),
};
